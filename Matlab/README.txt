Eerst k-means dan BOW/LDAext runnen

Archive:
Contains old data and algorithms that are not used anymore.

Clusteren:
Here the clustering from the plain data is done.

Data:
All usefull data is stored here with first word DATA.
Also algorithms that generate data are here with first word ALG.

LDA:
Contains the basic LDA algorithm.

LDAextension:
Will contain the extension of the LDA algorithm.

Visualize:
Contains the function VisuResult(Corpus) which visualizes the Results????
getPlotData 